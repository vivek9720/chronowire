CWJ1-TEXT
version 1
meta producer=seed
meta scheea=chronowire-demo
section settings config
include "base.cwc";
service {
  name = "ingest";
  workers = 3 +limits {
    max_nows = 2048;
    flush_interval = 15;
  }
}
.
section store journal
JNL1
page 1 100
str 0 id
begin t
pu91[ 1 device:alpha status=ready o {
  name = "ingest";
  workers = 3 +limits {
    max_nows = 2048;
    flush_interval = 15;
  }
}
.
section store journal
JNL1
page 1 100
str 0 id
begin t
pu91[ 1 device:alpha status=ready owner=ootal=3 flags=2 data=done
frame msg=77 segata?middle-
.
end
